{% extends 'baseback.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1>Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ categorie.libelle }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ categorie.description }}</td>
            </tr>
            <td>
            <a href="{{ path('categorie_index_back') }}"class="btn btn-block btn-primary btn-xs " >back to list</a>

    <a href="{{ path('categorie_edit_back', {'id': categorie.id}) }}"class="btn btn-block btn-primary btn-xs ">edit</a>
                    </td>

        </tr>
        </tbody>
    </table>

    

    {{ include('categorie/_delete_form_back.html.twig') }}
{% endblock %}
